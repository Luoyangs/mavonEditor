import {
  addDecimalNumber
} from '../number';

describe('number', () => {
  it('addDecimalNumber', () => {
    const result = addDecimalNumber(1.1, 2.01, 3.001);
    expect(result).to.equal(6.111);
  });
});
