import { ShopeeUIComponent } from '../../types/common';

export class Icon extends ShopeeUIComponent {
  // svg content of icon
  svg: string
  /**
   * whether spin the icon
   */
  spin: boolean
}

export default Icon;
