import { ShopeeUIComponent } from '../../types/common';

type ShopeeOtpInputTheme = 'underline' | 'box';

export class OtpInput extends ShopeeUIComponent {
  // the length of otp input
  length: number

  // the theme of otp input
  theme: ShopeeOtpInputTheme

  // whether the otp input is disabled
  disabled: boolean
}

export default OtpInput;
