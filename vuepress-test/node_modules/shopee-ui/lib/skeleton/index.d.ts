import { ShopeeUIComponent } from '../../types/common';

type ShopeeSkeletonType = 'line' | 'square' | 'circle';

export class Skeleton extends ShopeeUIComponent {

  // the type of skeleton
  type: ShopeeSkeletonType

  // show half of skeleton, only when type is line
  half: boolean
}

export default Skeleton;
