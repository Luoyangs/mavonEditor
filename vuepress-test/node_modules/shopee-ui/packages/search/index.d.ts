import { ShopeeUIComponent } from '../../types/common';

type ShopeeInputRestrictionType = 'value' | 'input';
type RestrictionFunction = (val: string) => boolean;
export class Search extends ShopeeUIComponent {
  value: number | string
  placement: string
  disabled: boolean
  clearable: boolean
  searchWhenClear: boolean
  placeholder: string
  // size of input: large large normal small
  size: string
  suffixIcon: string
  maxWidth: number
  maxHeight: number
  // same with shop-input attr
  restriction: string | RestrictionFunction | RegExp
  restrictionType: ShopeeInputRestrictionType
}

export { Option, OptionGroup } from 'shopee-ui/lib/select';

export default Search;
